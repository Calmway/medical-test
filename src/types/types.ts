export interface SessionType {
    id: number,
    name: string,
    selected: boolean
}
export interface Dialyzer {
    id: number,
    name: string
}
export interface Concentrator {
    id: number,
    name: string
}